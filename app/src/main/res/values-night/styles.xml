<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimaryLight</item>
        <item name="colorOnPrimary">@color/colorOnPrimaryLight</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorSecondaryLight</item>
        <!-- For Android sdk versions 26+. -->
        <item name="android:fontFamily" tools:targetApi="jelly_bean">@font/opensans_font</item>
        <!-- Target Android sdk versions < 26 and > 14. -->
        <item name="fontFamily">@font/opensans_font</item>
        <!-- sets the style for the overflow button -->
        <item name="actionOverflowButtonStyle">@style/actionOverflowButtonStyle</item>

        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox
        </item>
    </style>

    <style name="AppTheme" parent="Base.AppTheme" />

    <style name="Toolbar" parent="ThemeOverlay.AppCompat.DayNight">
        <item name="android:textColorPrimary">@color/colorOnPrimaryLight</item>
        <item name="android:textColorSecondary">@color/colorOnSecondaryLight</item>
        <item name="actionMenuTextColor">@color/colorOnPrimaryLight</item>
    </style>

    <!-- This style defines the tint color of the overflow menu button -->
    <style name="actionOverflowButtonStyle" parent="@style/Widget.MaterialComponents.ExtendedFloatingActionButton.Icon">
        <item name="android:tint">?attr/colorAccent</item>
    </style>

    <!-- Basic styling for the buttons. -->
    <style name="SleepButtons" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textColor">@color/colorOnSecondaryLight</item>
        <item name="colorAccent">?attr/colorAccent</item>
        <item name="colorControlHighlight">@color/colorOnSecondaryLight</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>